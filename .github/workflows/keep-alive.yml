name: Keep Supabase Alive
on:
  schedule:
    - cron: '0 0 */5 * *'   # runs every 5 days
  workflow_dispatch:         # allows manual run from the Actions tab too

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase (read-only, no data is added)
        run: |
          curl "https://lcsaclmerxmhfqtarkxn.supabase.co/rest/v1/registrations?select=id&limit=1" \
          -H "apikey: sb_publishable_HMZFgnCf5DZ4ropMDswdHg_zkq4eWej" \
          -H "Authorization: Bearer sb_publishable_HMZFgnCf5DZ4ropMDswdHg_zkq4eWej"
